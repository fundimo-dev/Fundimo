DATABASE_URL=postgresql://ledgerlens:ledgerlens@localhost:5432/ledgerlens?schema=public
JWT_SECRET=change-me-jwt-secret
CSRF_SECRET=change-me-csrf-secret
CORS_ORIGIN=http://localhost:3000
NODE_ENV=development

# Plaid Sandbox (optional; set all three or leave all unset). https://dashboard.plaid.com/developers/keys
# PLAID_CLIENT_ID=your_client_id
# PLAID_SECRET=your_sandbox_secret
# PLAID_ENV=sandbox
# PLAID_REDIRECT_URI=  # optional in sandbox; required for many OAuth institutions in development/production
# PLAID_OAUTH_CONTINUE_URI=https://app.fundimo.com/plaid/continue  # optional; backend redirect target back to app domain

# Required when using Plaid: 32-byte key (base64). Generate: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# APP_ENCRYPTION_KEY=
